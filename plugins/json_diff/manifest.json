{
  "name": "json_diff",
  "version": "1.0.0",
  "pluginApiVersion": "^1",
  "description": "2つのJSON値を再帰的に比較して差分を出す。追加・削除・変更されたパスをドット記法(例 a.b[0].c)で列挙し、変更は旧値→新値を示す。配列・ネストオブジェクト・型変更に対応。外部依存なしの純粋計算で、結果を {added, removed, changed, same} のJSONで返す。",
  "author": "moriwo-dev-ai",
  "license": "AGPL-3.0",
  "permissions": {
    "network": false,
    "childProcess": false,
    "fsScope": "none"
  },
  "dependencies": [],
  "smoke": {
    "input": {}
  }
}
